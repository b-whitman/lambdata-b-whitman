lambdata_b_whitman

A Python data science utility package

including:

contingency_chi2()

A function that takes in two categorical features and returns crosstab and chi2 score

check_nulls()

A function that takes a dataframe and returns a list of how many null values are in each column